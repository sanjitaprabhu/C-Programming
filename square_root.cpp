#include <stdio.h>
#include <math.h>
int main()
{
	float a,root;
	printf("Enter Number to find Square Root: ");
	scanf("%f",&a);
	root=sqrt(a);
	printf("Square Root: %f",root);
	return 0;
}
